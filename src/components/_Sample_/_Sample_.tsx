import styles from './index.module.css';
import classNames from "classnames";

export type T_Sample_Props = {

};

export const _Sample_ = ({}: T_Sample_Props) => {
    return (
        <div className={classNames(styles._Sample_)}>
            <h1>_Sample_</h1>
        </div>
    );
};
